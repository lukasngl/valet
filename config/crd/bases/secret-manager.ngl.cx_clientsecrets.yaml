---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: clientsecrets.secret-manager.ngl.cx
spec:
  group: secret-manager.ngl.cx
  names:
    kind: ClientSecret
    listKind: ClientSecretList
    plural: clientsecrets
    shortNames:
    - cs
    singular: clientsecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClientSecret is the Schema for the clientsecrets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClientSecretSpec defines the desired state of ClientSecret
            properties:
              config:
                description: |-
                  Config contains provider-specific configuration.
                  Each provider defines its own schema and validates at runtime.
                x-kubernetes-preserve-unknown-fields: true
              provider:
                description: Provider specifies which provider to use (e.g., "azure",
                  "aws")
                minLength: 1
                type: string
              secretRef:
                description: SecretRef specifies the target Secret to create/update
                properties:
                  name:
                    description: Name of the secret to create/update
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - config
            - provider
            - secretRef
            type: object
          status:
            description: ClientSecretStatus defines the observed state of ClientSecret
            properties:
              activeKeys:
                description: ActiveKeys lists all non-expired credentials
                items:
                  description: ActiveKey represents a provisioned credential key
                  properties:
                    createdAt:
                      description: CreatedAt is when this key was provisioned
                      format: date-time
                      type: string
                    expiresAt:
                      description: ExpiresAt is when this key will expire
                      format: date-time
                      type: string
                    keyId:
                      description: KeyID is the provider-specific identifier for this
                        key
                      type: string
                  required:
                  - createdAt
                  - expiresAt
                  - keyId
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentKeyId:
                description: CurrentKeyId is the identifier of the active credential
                type: string
              failureCount:
                description: FailureCount tracks consecutive failures for observability
                type: integer
              lastFailure:
                description: LastFailure is the timestamp of the last failure
                format: date-time
                type: string
              lastFailureMessage:
                description: LastFailureMessage contains the error from the last failure
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the generation of the spec that was last processed.
                  Used to detect spec changes that require reprovisioning.
                format: int64
                type: integer
              phase:
                description: Phase represents the current lifecycle phase
                enum:
                - Pending
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
