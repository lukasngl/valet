{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-mock.fullname" . }}-metrics
  labels:
    {{- include "provider-mock.labels" . | nindent 4 }}
spec:
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "provider-mock.selectorLabels" . | nindent 4 }}
{{- end }}
